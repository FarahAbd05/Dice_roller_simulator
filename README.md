# 🎲 Dice Roller Simulator

This is a beginner Python project that simulates rolling a six-sided die using Python's `random` module. The user can roll the dice repeatedly until they choose to stop.

---

## 🛠 Features

- Simulates a roll of a 6-sided dice
- Uses `random.randint()` to generate results
- Loops until the user chooses to quit
- Clean and simple terminal interface
